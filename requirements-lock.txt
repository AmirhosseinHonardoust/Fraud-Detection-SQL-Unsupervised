# Exact, fully-resolved runtime dependency versions verified against the test
# suite and used to regenerate outputs/. Resolved for cp311 (the Python
# version pinned in pyproject.toml and .github/workflows/ci.yml), consistent
# with the ranges in requirements.txt.
#
# requirements.txt stays range-pinned (the flexible spec for consumers);
# this file is the reproducible one for CI or anyone who wants an exact match.
#
# Install with: pip install -r requirements-lock.txt
cloudpickle==3.1.2
contourpy==1.3.2
cycler==0.12.1
fonttools==4.64.0
joblib==1.6.0
kiwisolver==1.5.1
matplotlib==3.11.1
numpy==2.2.6
packaging==26.3
pandas==2.3.2
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
scikit-learn==1.7.2
scipy==1.16.3
six==1.17.0
threadpoolctl==3.6.0
tzdata==2026.3
